/* eslint-disable prettier/prettier */
import styled from 'styled-components';
import * as theme from '../../../theme';

export const NotyfyStyled = styled('div')`
  font-size: ${theme.FONTS.SIZES.m};
  text-align: center;

`;
